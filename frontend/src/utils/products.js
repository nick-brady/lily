// What each physical product is called in prose, per product_kind — "on the
// mug", "on the print". One place, so a new product gets its noun once.
export const PRODUCT_NOUN = { mug: 'mug', framed_print: 'print', ornament: 'ornament' };
