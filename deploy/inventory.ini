# ⚠ Placeholder IP — no production box exists yet. Update ansible_host
# (and the key path if it changes) once the EC2 instance is provisioned.
[lily]
arrivalstory.com ansible_host=52.25.89.85 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/lily.pem

[lily:vars]
ansible_python_interpreter=/usr/bin/python3
