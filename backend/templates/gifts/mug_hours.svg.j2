<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink"
     width="{{ w }}" height="{{ h }}" viewBox="0 0 {{ w }} {{ h }}">
  <rect x="0" y="0" width="{{ w }}" height="{{ h }}" fill="{{ p.bg }}"/>

  <!-- the labor clock, left face of the wrap -->
  {% for t in clock_ticks %}
  <line x1="{{ t.x1 }}" y1="{{ t.y1 }}" x2="{{ t.x2 }}" y2="{{ t.y2 }}"
        stroke="{{ p.ink }}" stroke-width="{{ 3 if t.hour else 1.6 }}"
        opacity="{{ 0.38 if t.hour else 0.16 }}"/>
  {% endfor %}
  {% for s in clock_strokes %}
  <line x1="{{ s.x1 }}" y1="{{ s.y1 }}" x2="{{ s.x2 }}" y2="{{ s.y2 }}"
        stroke="{{ p.accent }}" stroke-width="4.5" stroke-linecap="round"
        opacity="{{ s.o }}"/>
  {% endfor %}
  {% if clock_start_dot %}
  <circle cx="{{ clock_start_dot.x }}" cy="{{ clock_start_dot.y }}" r="9"
          fill="{{ p.dot }}"/>
  {% endif %}
  {% if clock_star %}
  <path d="{{ clock_star }}" fill="{{ p.accent }}"/>
  {% endif %}
  {% if clock_star_label %}
  <text x="{{ clock_star_label.x }}" y="{{ clock_star_label.y }}" text-anchor="middle"
        font-family="{{ p.body_font }}" font-size="28" letter-spacing="3"
        fill="{{ p.ink }}" opacity="0.55">{{ birth_time }}</text>
  {% endif %}
  {% if clock_start_label and labor_start_time %}
  <text x="{{ clock_start_label.x }}" y="{{ clock_start_label.y }}" text-anchor="middle"
        font-family="{{ p.body_font }}" font-size="28" letter-spacing="3"
        fill="{{ p.ink }}" opacity="0.55">{{ labor_start_time }}</text>
  {% endif %}

  <!-- name + date, right face of the wrap -->
  <text x="1360" y="500" font-family="{{ p.display_font }}" font-size="175"
        font-weight="300" font-style="italic" fill="{{ p.ink }}">{{ child_name }}</text>
  <text x="1366" y="600" font-family="{{ p.body_font }}" font-size="38"
        letter-spacing="2" fill="{{ p.ink }}" opacity="0.62">born {{ birth_date }} at {{ birth_time }}</text>

  <line x1="1366" y1="700" x2="1526" y2="700" stroke="{{ p.ink }}"
        stroke-width="2" opacity="0.25"/>
  <text x="1366" y="785" font-family="{{ p.body_font }}" font-size="29"
        letter-spacing="5" fill="{{ p.ink }}"
        opacity="0.5">{{ count }} CONTRACTIONS · {{ labor_duration|upper }} · EVERY {{ avg_interval|upper }}</text>
</svg>
